// write the custom function for endsWith()

let str = "Ankit paswan";
let char = "n";

let endsWith = str.lastIndexOf(char) === str.length - 1;

console.log(endsWith);

// function endWith(str, char) {
//   return str.lastIndexOf(char) === str.length - 1;
// }
// console.log(endWith(str, char));
